---
name: rubocop

"on":
  pull_request:

jobs:
  release:
    runs-on: ubuntu-20.04

    steps:
      - name: Checkout source
        uses: actions/checkout@v2

      - name: Rubocop checks
        uses: gimenete/rubocop-action@1.0

...
